export default function RoomsPage() {
  return (
    <div className="mx-auto max-w-5xl space-y-3 px-4 py-8">
      <h1 className="text-2xl font-bold">Phòng học</h1>
      <p className="text-gray-600">
        Danh sách phòng học sẽ hiển thị ở đây (gọi API <code>/api/v1/rooms/</code>).
      </p>
    </div>
  );
}
